export class Note {
    title: string;
    id: string;
    text: string;

    constructor(title: string, id: string, text: string) {
        this.title = title;
        this.id = id;
        this.text = text;
    }
}
